
AOS.init(
    //duration: 400
);


let one_arch_img = document.querySelector('.one_arch_img_')
let arch_titile = document.querySelector('.arch_titile')


/* 

setTimeout(() => {
    arch_titile.style.display = "block"
    arch_titile.style.scale = 1
    arch_titile.style. translateY =(0 + "px")

}, 1000);
 */
/* 
window.onscroll = () => {
    let y = window.scrollY
    stars.style.margin = (y * 0.45) + "px"
    moon.style.marginTop = (y * 1.30) + "px"
    ges.style.marginTop = (y * 0.55) + "px"
    text.style.marginRight = (y * 0.70) + "px"
    btn.style.marginTop = (y * 1) + "px"
} */
/* 
window.onscroll = () => {
    let y = window.scrollY

    one_arch_img.style.borderRadius = (y * 10) + "%"

} */
